2) HP-UX stability

3) MacOS X disk_io, page and general stats

4) configure.ac - xpg\d vs. their funcs
   * ac_check_funcs ...

6) DragonflyBSD & NetBSD testing
   ==> done?

7) FreeBSD memory stats; totals don't add up
   ==> done?

8) define __NEED_SG_GET_SYS_PAGE_SIZE only when needed
